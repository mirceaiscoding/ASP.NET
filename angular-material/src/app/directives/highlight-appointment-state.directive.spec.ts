import { HighlightAppointmentStateDirective } from './highlight-appointment-state.directive';

describe('HighlightAppointmentStateDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightAppointmentStateDirective();
    // expect(directive).toBeTruthy();
  });
});
