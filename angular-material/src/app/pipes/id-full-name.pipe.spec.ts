import { IdFullNamePipe } from './id-full-name.pipe';

describe('IdFullNamePipe', () => {
  it('create an instance', () => {
    const pipe = new IdFullNamePipe();
    expect(pipe).toBeTruthy();
  });
});
